import { TEmploee } from "./src/scripts/types";

//1 task
/*import { seasons } from "./src/scripts/data";
import { TSeason } from "./src/scripts/types";
//1 задание - самый прибыльный
let maxIncome: TSeason | null = null;
let minIncome: TSeason | null = null;
let moreHired: TSeason | null = null;
let moreDismissed: TSeason | null = null;
let maxSeasonName: string | null = null;
let minSeasonName: string | null = null;
let moreDismissedName: string | null = null;

let moreHiredName: string | null = null;
Object.entries(seasons).forEach(([season, seasonInfo]) => {
  if (!maxIncome || maxIncome.income <= seasonInfo.income) {
    maxIncome = seasonInfo;
    maxSeasonName = season;
  }
  if (!minIncome || minIncome.income >= seasonInfo.income) {
    minIncome = seasonInfo;
    minSeasonName = season;
  }
  if (!moreHired || moreHired.hired >= seasonInfo.hired) {
    moreHired = seasonInfo;
    moreHiredName = season;
  }
  if (!moreDismissed || moreDismissed.dismissed >= seasonInfo.dismissed) {
    moreDismissed = seasonInfo;
    moreDismissedName = season;
  }
});
console.log(maxSeasonName, maxIncome);
console.log(minSeasonName, minIncome);
console.log(moreHiredName, moreHired);
console.log(moreDismissedName, moreDismissed);*/

//import { testString, addNumber } from "./src/scripts/script";
/*import * as testFile from "./src/scripts/script";
console.log(testFile.addNumber());*/
